version: "3.9"
services:
  recipe-box:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        PORT: 3000
    ports:
      - "${APP_PORT:-3000}:3000"
    environment:
      - NODE_ENV=production